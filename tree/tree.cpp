#include <iostream>
using namespace std;

struct Tnode {
	int _val;
	Tnode* _lson, * _rson;
};

int find_mid(Tnode* root)
{
	if (!root)
		return 0;


}

int main()
{
	Tnode* root = nullptr;
	find_mid(root);

	return 0;
}
